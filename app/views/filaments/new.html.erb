<%# new page renders a partial in the hopes partial may be made into a modal in the future %>
<%= render partial: 'shared/add_filament', locals: { user: current_user, filament_types: @filament_types, filament_colors: @filament_colors } %>